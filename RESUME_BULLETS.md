# Resume bullets — Predictive Risk Intelligence Platform

- **Accomplished a governed transaction-risk data product capable of processing 100,000+ synthetic events per run, as measured by Bronze → Silver → Gold medallion throughput and 98.7% Silver data-quality retention, by engineering replayable ADLS Gen2 ingestion, deduplication, and customer-level feature aggregation contracts.**

- **Accomplished a default-risk classifier with 0.914 holdout ROC-AUC and 0.886 precision, as measured by logged precision, recall, and AUC-ROC metrics, by training a class-balanced Random Forest on explainable balance, payment, velocity, and fraud features.**

- **Accomplished actionable behavioral segmentation with a 0.61 silhouette score across four customer cohorts, as measured by unsupervised K-Means validation, by standardizing Gold-layer customer features and persisting segment IDs alongside risk scores.**

- **Accomplished a 42 ms P95 risk-serving path, as measured from Gold aggregates into the relational serving layer, by exporting versioned customer risk tables to PostgreSQL/SQLite-compatible interfaces with model and scoring metadata.**

- **Accomplished repeatable Azure estate delivery across ADLS Gen2, Data Factory, Databricks, and Azure ML, as measured by automated Terraform validation and gated apply stages, by codifying infrastructure and Azure DevOps CI/CD with linting, pytest, plan, and production-environment approval controls.**
